# Showcase ROM Source

This directory contains the showcase ROM source and ROM-side generated data.

Current showcase entry point:
- `showcase.asm`
  Fixed-ROM boot code plus banked scene-init routines for the title,
  compositing, tile-mode, and sprite verification checkpoints.
