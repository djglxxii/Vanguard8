# Showcase Milestone 4 — Mixed-Mode and Graphic 4 Command-Engine Verification Scenes

Objective:
- Add the remaining planned video scenes that verify covered Graphic 3/4
  mixed-mode behavior and Graphic 4 command-engine use.

Required docs:
- `docs/spec/02-video.md`
- `showcase/docs/showcase-rom-plan.md`
- `showcase/docs/showcase-implementation-plan.md`

Allowed scope:
- Mixed-mode scene, command-engine scene, their generated source assets, and
  updated checkpoint metadata.

Explicit non-goals:
- Audio verification or final regression freeze work.
- Any reliance on undocumented Graphic 3 lines `192-211` or unverified
  horizontal-scroll behavior.

Allowed paths:
- `showcase/`

Required verification:
- Mixed-mode scene stays within the documented covered path.
- Command-engine scene produces a visually obvious deterministic change.
- Checkpoints for both scenes are documented for later regression use.

Exit criteria:
- A reviewer can inspect mixed-mode and command-engine captures and confirm the
  scenes are validating the intended behaviors without crossing into deferred
  hardware areas.

Forbidden extras:
- Any use of Graphic 5/6/7, text modes, or unverified scroll/parallax.

Verification commands:
```bash
python3 showcase/tools/package/build_showcase.py
python3 showcase/tools/package/run_showcase_headless.py --frames 960 --trace build/showcase/m4.trace --hash-frame 959 --symbols build/showcase/showcase.sym
```

