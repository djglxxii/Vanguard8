# Showcase Milestone 6 — System Validation Scene, Regression Manifests, and Freeze

Objective:
- Finish the ROM as a practical regression artifact by adding the system
  validation scene, freezing checkpoint manifests, and documenting the final
  review handoff.

Required docs:
- `docs/spec/00-overview.md`
- `docs/spec/01-cpu.md`
- `docs/spec/02-video.md`
- `docs/spec/03-audio.md`
- `docs/spec/04-io.md`
- `showcase/docs/showcase-rom-plan.md`
- `showcase/docs/showcase-implementation-plan.md`

Allowed scope:
- System validation scene, final checkpoint manifests, replay inputs if needed,
  and the final showcase review notes.

Explicit non-goals:
- New showcase features not already planned.
- Emulator-core work, CI expansion, or post-freeze polish beyond review fixes.

Allowed paths:
- `showcase/`

Required verification:
- The full ROM loop can be built and run from the documented showcase commands.
- Final frame/audio/trace checkpoint manifests are present and reviewable.
- Human review notes explain how to inspect the full loop.

Exit criteria:
- A reviewer can rebuild the ROM, run the full loop, and verify the checkpoint
  manifests without relying on unwritten tribal knowledge.

Forbidden extras:
- Any new scene or subsystem not already committed by earlier accepted
  milestones.

Verification commands:
```bash
python3 showcase/tools/package/build_showcase.py
python3 showcase/tools/package/run_showcase_headless.py --frames 1440 --trace build/showcase/m6.trace --hash-frame 1439 --hash-audio --symbols build/showcase/showcase.sym
```

