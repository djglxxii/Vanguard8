# vcpkg Placeholder

Milestone 0 scaffolds the expected `third_party/vcpkg` path so the documented
toolchain command resolves during early bootstrap. Replace this directory with
the real vcpkg submodule before relying on vcpkg-managed dependencies.
